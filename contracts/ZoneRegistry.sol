pragma solidity ^0.4.13;

contract ZoneRegistry {

    function ZoneRegistry() {
        
    }

    // mapping (_KeyType => _ValueType) mapName

    // function requestProof(bytes16 uuid) returns (string r) {
    //    r = a * _multiplier;
    //    Multiplied(a, msg.sender, r);
    //    return r;
    // }
 }